// Pogram to check Odd or even  number.
#include<stdio.h>
#include<conio.h>
int main()
{
	int number;
	printf("Enter the number");
	scanf("%d",&number);
	if (number % 2 == 0){

	printf("%d the number is even",number);}
	else {
		("%d the number is odd", number);
	}
	
}